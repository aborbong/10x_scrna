params {
    projectDir = "$PWD/10x_scrna"
    reads = "$PWD/data/reads/_*R{1,2}_.fastq.gz"
    transcriptome = "${params.projectDir}/data/ref/"
    outdir = "${params.projectDir}/results"
    fastqc = "${outdir}/fastqc"
}

