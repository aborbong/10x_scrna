params {
    projectDir = "$PWD/"
    reads = "$PWD/data/reads/*{R1,R2}*.fastq.gz"
    transcriptome = "${params.projectDir}/data/ref/"
    outdir = "${params.projectDir}/results"
    fastqc = "${outdir}/fastqc"
    cellranger_count = "${params.outdir}/cellranger_count"
}

